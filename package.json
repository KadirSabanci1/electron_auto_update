{
  "name": "electron_auto_update",
  "version": "1.0.1",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always",
    "electron-package": "yarn build && electron-builder build --publish never"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "electron-log": "^4.3.2",
    "electron-updater": "^4.0.6"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/KadirSabanci1/electron_auto_update.git"
  }
}
